; ─────────────────────────────────────────────────────────────
; PHP CONFIGURATION FOR ADMIN PANEL (FastCGI/FPM)
; ─────────────────────────────────────────────────────────────
; File này dùng để thay thế cấu hình trong .htaccess cho các 
; server không hỗ trợ 'php_value'.

max_file_uploads = 100
post_max_size = 512M
upload_max_filesize = 50M
memory_limit = 256M
